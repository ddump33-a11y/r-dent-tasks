# R-Dent Tasks — Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# === Email (M365 / SMTP) ===
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=your-email@r-dent.com
SMTP_PASS=your-app-password

# === App ===
PORT=3000
